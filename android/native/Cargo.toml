[package]
name = "scout-operator-native"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "=0.21.1"
tokio = { version = "=1.53.1", features = ["rt"] }
wallet-engine = { path = "../../crates/wallet-engine" }

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"

[workspace]
